---
name: phase-research
description: >
  対象Phaseが既存コードへどう接続されるかを調査し、entry point・実行flow・既存pattern・test・制約を根拠付きで整理する。
---

# Phase Research

詳細手順は同じディレクトリの `REFERENCE.md` を参照する。

`REFERENCE.md` は移行前本文のため、frontmatterは無視し、本文中のすべての `*-ja` Skill名は `-ja` を除去して解釈する。`.claude/skills` への参照がある場合は `.myskills` と読み替える。
