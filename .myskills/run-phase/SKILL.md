---
name: run-phase
description: >
  承認済みPhaseについて、State復元 → Research → Task Planning → Implementation → Verification → Independent Review → Repair → Phase Closeを順番にOrchestrationする。
---

# Run Phase Workflow

詳細手順は同じディレクトリの `REFERENCE.md` を参照する。

`REFERENCE.md` は移行前本文のため、frontmatterは無視し、本文中のすべての `*-ja` Skill名は `-ja` を除去して解釈する。`.claude/skills` への参照がある場合は `.myskills` と読み替える。
